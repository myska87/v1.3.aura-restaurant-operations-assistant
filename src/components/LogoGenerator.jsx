import React from "react";

// DISABLED: Logo generation removed to improve performance
// Logo will use fallback design in AuraLogo component
export default function LogoGenerator({ onLogoGenerated }) {
  return null;
}